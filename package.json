{
  "name": "your-project-name",
  "private": true,
  "scripts": {
    "start": "npm --prefix my-ap start",
    "build": "npm --prefix my-ap run build",
    "test": "npm --prefix my-ap test -- --watchAll=false"
  },
  "engines": {
    "node": "18.x"
  }
}
